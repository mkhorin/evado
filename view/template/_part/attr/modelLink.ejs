<!-- _part/attr/modelLink -->
<%
const relModel = model.rel(attr);
const id = model.get(attr);
%>
<%- include(_view.get('_part/attr/static'), {
  displayValue: relModel ? relModel.getTitle() : undefined,
  url: id ? [url, {id}] : undefined
}) %>