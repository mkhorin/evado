<!-- _part/attr/modelLink -->
<%
let relModel = model.rel(attr);
%>
<%- include('static', {
    displayValue: relModel ? relModel.getTitle() : undefined,
    url: relModel ? [url, model.rel(attr)] : undefined
}) %>