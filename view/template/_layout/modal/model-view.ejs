<!-- _layout/modal/model-view -->
<%
const data = {
    id: model.getId(),
    key: model.PK
};
if (typeof params !== 'undefined') {
    Object.assign(data, params);
}
%>
<div class="jmodal-scroll-container content-box box-info box" data-title="<%= blocks.title %>">

    <div class="box-header with-border scroll-header">
        <div class="model-controls">

            <button data-id="copyId" class="btn btn-white" type="button" title="Copy ID">ID</button>

            <button data-id="reload" class="reload btn btn-default" type="button" title="Reload">
                <span class="fa fa-sync-alt"></span>
            </button>

            <%- include(_view.get('_part/tool/model-history')) %>
            <%- include(_view.get('_part/misc/universal-render'), {data: _data.get('customControls')}) %>

            <% if (_data.get('help')) { %>
            <span class="model-help fa fa-question-circle" title="<%= _data.get('help') %>"></span>
            <% } %>
        </div>
    </div>
    <div class="scroll-body box-body">

        <div class="box-notice"></div>

        <div class="model-view model form-horizontal form"
             data-jam="Model"
             data-params="<%= JSON.stringify(data) %>">
            <%- body -%>
        </div>
    </div>
</div>